const arr1=[1,5,8,16]
console.log(arr1);
const result= arr1.map((num)=>
num*2);

console.log(result);

// map function is use to iterate over the array and perform the operation on each element of the array and 
// return the new array with the modified values
//the array length is same as the original array but the values are modified according to the function we have
//  passed in the map function